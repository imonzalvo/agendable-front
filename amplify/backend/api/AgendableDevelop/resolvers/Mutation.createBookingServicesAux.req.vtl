$util.qr($ctx.stash.put("res", $context.prev.result))

#set($payload = $utils.toJson($context.arguments))
{
    "version": "2017-02-28",
    "operation": "Invoke",
    "payload": {
        "type": "Mutation",
        "field": "appVersion",
        "arguments": $payload,
        "bookingId":  $utils.toJson($context.prev.result.id),
        "identity": $utils.toJson($context.identity)
    }
}